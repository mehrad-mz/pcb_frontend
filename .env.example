# Django API origin used by Next.js rewrites in dev and Docker.
# Point this at your backend server (e.g. http://localhost:8000 or https://api.example.com).
DJANGO_API_ORIGIN=http://localhost:8000

# Optional: public API base URL for client-side fetch (leave empty to use same-origin /api).
NEXT_PUBLIC_API_URL=
